void strTableTwo(char *src, char *dst, int srcLen, int dstLen) {
	int i;	int ip;
	for ( i = dstLen-1; i >= 0; i-- ) {
		ip = dst[i] % 23;
		if (ip <= srcLen) {
			src[ip] = dst[i];

		}
	}
	return;
}
